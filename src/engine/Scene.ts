import GameObject from "./GameObject";

class Scene {
  // public active: boolean = false;
  // public id: string;
  // public collection: GameObject[] = [];

  // constructor(id: string) {
  //   this.id = id;
  // }

  // public addObject(id: string) {
  //   const idExists = this.collection.find(obj => obj.id === id);

  //   if (idExists) throw new Error(`A object with id: ${id} already exists!`);

  //   const obj = new GameObject(id);
  //   this.collection.push(obj);

  //   return obj;
  // }

  // public object(id: string) {
  //   return this.collection.find(obj => obj.id === id);
  // }
}

export default Scene;